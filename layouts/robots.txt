{{ if hugo.IsProduction }}
  # Production: allow indexing of the whole site
  User-agent: *
  Disallow:
{{ else }}
  # Non-production (preprod/dev): block all robots, this content is not meant to be indexed
  User-agent: *
  Disallow: /
{{ end }}

Sitemap: {{ "sitemap.xml" | absLangURL }}
